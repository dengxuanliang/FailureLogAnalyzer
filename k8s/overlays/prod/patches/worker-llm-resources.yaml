apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-llm
spec:
  template:
    spec:
      containers:
        - name: worker-llm
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 2Gi
